main menu with example play

finish swapping, checking for matches, removing and refilling matches - removeBlocks will push to a tobe removed array so that
the timer function can remove and animate

sprites

animations - rotation for eliminated blocks
	*** there will be double counting in the g['toRemove'] array, but a simple search for applying the rotation should
	*** ignore this. 

???

